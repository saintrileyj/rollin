# Robinhood credentials (used only when broker = robinhood)
ROBINHOOD_USERNAME=
ROBINHOOD_PASSWORD=
# Optional TOTP secret if 2FA is enabled (the base32 secret, not a 6-digit code)
ROBINHOOD_MFA_SECRET=

# Alpaca credentials (recommended Cash-App-style alternative; paper or live)
ALPACA_KEY_ID=
ALPACA_SECRET_KEY=
ALPACA_BASE_URL=https://paper-api.alpaca.markets
